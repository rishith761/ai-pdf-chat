# Copy to .env and set values as needed
PORT=3000
HOST=0.0.0.0
# Optional: set for AI responses (OpenAI key)
# OPENAI_API_KEY=sk-...
# Optional: enable S3 presigned uploads (recommended for production)
# AWS_REGION=us-east-1
# S3_BUCKET=your-bucket-name
# If you want to require a key to generate presigned URLs, set UPLOAD_KEY
# UPLOAD_KEY=some-secret-key
